on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Bun
      - uses: oven-sh/setup-bun@v1
      - name: bun install, build and test
        run: |
          bun install
          bun test
