on:
	push:
		branches:
		- main

jobs:
	test:
 		name: testing the app
		runs-on: ubuntu-22.04

  		steps:
  		- name: Setup Bun
		- uses: oven-sh/setup-bun@v1

  		- name: bun install, build and test
		run: |
  			bun install
	 			bun test
